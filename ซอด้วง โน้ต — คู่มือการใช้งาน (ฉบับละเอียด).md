# คู่มือ Sawduang Notes (ซอด้วง โน้ต)
## ฉบับละเอียด — สำหรับคนที่ไม่เคยเขียนโปรแกรมมาก่อน

---

**เวอร์ชัน:** 1.0  
**อัปเดตล่าสุด:** 16 พฤษภาคม 2026  
**สร้างโดย:** ใบบุญเอง (Bai)  
**Repo:** https://github.com/Zintrea/leaves-notes-web

---

## 📑 สารบัญ

- [1. เว็บนี้คืออะไร?](#1-เว็บนี้คืออะไร)
- [2. เทคโนโลยีที่ใช้ — สร้างจากอะไรบ้าง?](#2-เทคโนโลยีที่ใช้--สร้างจากอะไรบ้าง)
- [3. โครงสร้างไฟล์ — แต่ละโฟลเดอร์คืออะไร?](#3-โครงสร้างไฟล์--แต่ละโฟลเดอร์คืออะไร)
- [4. การรันเว็บบนเครื่องตัวเอง (Local)](#4-การรันเว็บบนเครื่องตัวเอง-local)
- [5. การเพิ่มเพลงใหม่](#5-การเพิ่มเพลงใหม่)
- [6. การแก้ไขเพลงที่มีอยู่](#6-การแก้ไขเพลงที่มีอยู่)
- [7. การลบเพลง](#7-การลบเพลง)
- [8. การเปลี่ยนราคาแพ็กเกจ](#8-การเปลี่ยนราคาแพ็กเกจ)
- [9. การเพิ่ม/ลบแพ็กเกจ](#9-การเพิ่มลบแพ็กเกจ)
- [10. การเปลี่ยนรูปภาพ](#10-การเปลี่ยนรูปภาพ)
- [11. การเปลี่ยนลิงค์ Social Media](#11-การเปลี่ยนลิงค์-social-media)
- [12. การเปลี่ยนสีและฟอนต์](#12-การเปลี่ยนสีและฟอนต์)
- [13. การเปลี่ยนข้อความในหน้าเว็บ](#13-การเปลี่ยนข้อความในหน้าเว็บ)
- [14. การเพิ่มไฟล์ PDF โน้ตเพลง](#14-การเพิ่มไฟล์-pdf-โน้ตเพลง)
- [15. การ Deploy ขึ้น Netlify (ทำให้คนอื่นดูได้)](#15-การ-deploy-ขึ้น-netlify-ทำให้คนอื่นดูได้)
- [16. การแก้ไขปัญหาเบื้องต้น](#16-การแก้ไขปัญหาเบื้องต้น)
- [17. Glossary — คำศัพท์ที่ควรรู้](#17-glossary--คำศัพท์ที่ควรรู้)

---

## 1. เว็บนี้คืออะไร?

**Sawduang Notes (ซอด้วง โน้ต)** เป็นเว็บไซต์สำหรับแชร์โน้ตเพลงซอด้วง ที่ใบบุญเองทำขึ้น

**สิ่งที่เว็บนี้มี:**
- รวมโน้ตเพลงซอด้วง — ทั้งฟรีและเสียเงิน
- ค้นหาและกรองเพลงตามระดับความยากและประเภท
- แพ็กเกจสั่งทำโน้ต (แบบที่ 1, 2, 3)
- ลิงค์ติดต่อสำหรับสั่งทำโน้ต

**ใครใช้เว็บนี้บ้าง?**
- คนที่เล่นซอด้วงและอยากได้โน้ตเพลง
- ใบบุญเอง (สำหรับอัปเดตเพลงและข้อมูล)

---

## 2. เทคโนโลยีที่ใช้ — สร้างจากอะไรบ้าง?

ไม่ต้องเข้าใจลึกก็ได้ แต่รู้ไว้เผื่อมีคนถาม:

| ชื่อ | คืออะไร? | เปรียบเทียบง่าย ๆ |
|------|---------|-------------------|
| **React** | ตัวสร้างหน้าตาเว็บ | เหมือนตัวต่อ LEGO ที่เอามาต่อกันเป็นหน้าเว็บ |
| **TypeScript** | ภาษาที่ใช้เขียน | เหมือนภาษาไทยแต่มีไวยากรณ์ชัดเจนขึ้น |
| **Vite** | ตัวแปลงโค้ดให้เป็นเว็บ | เหมือนเครื่องปิ้งขนมปัง — ใส่โค้ดเข้าไป → ออกมาเป็นเว็บ |
| **Tailwind CSS** | ตัวจัดแต่งทรงผมให้เว็บ | เหมือนชุดคำสั่งแต่งสี ขนาด ระยะห่าง |
| **shadcn/ui** | ชุดตัวต่อ LEGO สำเร็จรูป | ปุ่ม, การ์ด, ช่องค้นหา ที่มีอยู่แล้ว |
| **Netlify** | ที่ฝากเว็บ | เหมือนฝากเว็บไว้บนตู้เย็นของคนอื่น แต่คนทั้งโลกดูได้ |
| **JSON** | ไฟล์เก็บข้อมูล | เหมือนสมุดจดรายชื่อเพลง — เปิดมาแก้ได้เลย |

---

## 3. โครงสร้างไฟล์ — แต่ละโฟลเดอร์คืออะไร?

```
leaves-notes-web/                     ← โฟลเดอร์หลักของโปรเจกต์
│
├── client/                           ← ทุกอย่างที่เกี่ยวกับหน้าตาเว็บ
│   ├── public/                       ← ไฟล์ที่คนดูเว็บเข้าถึงได้
│   │   ├── data/                     ← ★★★ ที่แก้ข้อมูลบ่อยที่สุด ★★★
│   │   │   ├── songs.json            ←    ข้อมูลเพลงทั้งหมด
│   │   │   ├── packages.json         ←    ข้อมูลแพ็กเกจ + ราคา
│   │   │   └── contacts.json         ←    ลิงค์ Social Media
│   │   └── pdfs/                     ← ★★★ ใส่ไฟล์ PDF โน้ตเพลง ★★★
│   │
│   ├── src/                          ← โค้ดของเว็บ (ไม่ต้องแก้บ่อย)
│   │   ├── components/               ← ชิ้นส่วนต่าง ๆ ของเว็บ
│   │   │   ├── layout/               ← ส่วนหัว (Navbar) + ส่วนท้าย (Footer)
│   │   │   ├── sections/             ← ส่วนต่าง ๆ ในหน้าแรก
│   │   │   │   ├── HeroSection.tsx   ←    ส่วนแนะนำเว็บ (รูป + ข้อความต้อนรับ)
│   │   │   │   ├── SongSection.tsx   ←    ส่วนแสดงรายการเพลง
│   │   │   │   └── OrderSection.tsx  ←    ส่วนสั่งทำโน้ต
│   │   │   ├── ui/                   ← ปุ่ม การ์ด ช่องค้นหา (ไม่ต้องแก้)
│   │   │   └── shared/               ← ErrorBoundary (ไม่ต้องแก้)
│   │   ├── pages/
│   │   │   └── Home.tsx              ← หน้าแรก (ไม่ต้องแก้)
│   │   ├── hooks/                    ← โค้ดดึงข้อมูล (ไม่ต้องแก้)
│   │   ├── services/                 ← โค้ดเชื่อมต่อข้อมูล (ไม่ต้องแก้)
│   │   ├── App.tsx                   ← ตัวตั้งค่าเว็บ (ไม่ต้องแก้)
│   │   ├── main.tsx                  ← จุดเริ่มต้นเว็บ (ไม่ต้องแก้)
│   │   └── index.css                 ← สี ฟอนต์ ทรงผมของเว็บ
│   └── index.html                    ← หน้า HTML หลัก (ไม่ต้องแก้)
│
├── .env.example                      ← ตัวอย่างค่าตั้งค่า (ไม่ต้องแก้)
├── netlify.toml                      ← ตั้งค่า Netlify (ไม่ต้องแก้)
├── package.json                      ← รายการเครื่องมือที่ใช้ (ไม่ต้องแก้)
├── vite.config.ts                    ← ตั้งค่า Vite (ไม่ต้องแก้)
├── tsconfig.json                     ← ตั้งค่า TypeScript (ไม่ต้องแก้)
│
├── ซอด้วง โน้ต — คู่มือการใช้งาน.pdf  ← ★★★ ไฟล์นี้ที่คุณกำลังอ่าน ★★★
│
└── .hermes/                          ← ไฟล์วางแผน (ไม่ต้องสนใจ)
```

### โฟลเดอร์ที่ต้องรู้จัก (สำหรับแก้ไขข้อมูล)

| โฟลเดอร์/ไฟล์ | ใช้ทำอะไร? | แก้บ่อยไหม? |
|--------------|-----------|------------|
| `client/public/data/songs.json` | ข้อมูลเพลงทั้งหมด | ✅ แก้บ่อยที่สุด |
| `client/public/data/packages.json` | ราคาและรายละเอียดแพ็กเกจ | ✅ แก้บ่อย |
| `client/public/data/contacts.json` | ลิงค์ Facebook, LINE, etc. | 🔄 แก้ครั้งเดียว |
| `client/public/pdfs/` | ใส่ไฟล์ PDF โน้ตเพลง | ✅ แก้บ่อย |
| `client/src/index.css` | สีและฟอนต์ของเว็บ | 🔄 ไม่ค่อยแก้ |

---

## 4. การรันเว็บบนเครื่องตัวเอง (Local)

### 4.1 ติดตั้งของที่ต้องใช้ก่อน

มี 2 อย่างที่ต้องติดตั้ง:

#### ① Node.js (ตัวรัน JavaScript)
1. เปิดเว็บ https://nodejs.org/
2. กดปุ่มสีเขียว **LTS** (ตัวเลขประมาณ 20.x.x)
3. เปิดไฟล์ที่โหลดมา → กด Next → Next → Install → Finish
4. **ตรวจสอบว่าติดตั้งสำเร็จ:**
   - Windows: กด `Windows + R` → พิมพ์ `cmd` → Enter
   - พิมพ์: `node --version`
   - ถ้าขึ้น `v20.x.x` หรือสูงกว่า → ✅ สำเร็จ

#### ② Git (ตัวจัดการเวอร์ชันโค้ด)
1. เปิดเว็บ https://git-scm.com/
2. กด Download → เปิดไฟล์ → Next → Next → Install → Finish
3. **ตรวจสอบ:**
   - เปิด cmd → พิมพ์ `git --version`
   - ถ้าขึ้น `git version 2.x.x` → ✅ สำเร็จ

#### ③ pnpm (ตัวจัดการแพ็กเกจ — เหมือน npm แต่เร็ว)
1. เปิด cmd → พิมพ์:
   ```
   npm install -g pnpm
   ```
2. **ตรวจสอบ:**
   ```
   pnpm --version
   ```
   ถ้าขึ้นตัวเลข → ✅ สำเร็จ

### 4.2 ดาวน์โหลดโค้ดจาก GitHub

1. เปิด cmd หรือ Terminal
2. พิมพ์:
   ```
   cd Desktop
   ```
3. พิมพ์:
   ```
   git clone https://github.com/Zintrea/leaves-notes-web.git
   ```
4. รอจนเสร็จ (ประมาณ 10-30 วินาที)
5. พิมพ์:
   ```
   cd leaves-notes-web
   ```

### 4.3 ติดตั้งเครื่องมือที่เว็บต้องใช้

พิมพ์:
```
pnpm install
```
รอประมาณ 1-3 นาที (ถ้าช้าเพราะอินเทอร์เน็ต)

### 4.4 เปิดเว็บ

พิมพ์:
```
pnpm dev
```

จะเห็นข้อความ:
```
➜  Local:   http://localhost:3000/
```

เปิดเบราว์เซอร์ → ไปที่ `http://localhost:3000` → จะเห็นเว็บซอด้วง โน้ต

### 4.5 หยุดเว็บ

ในหน้าต่าง Terminal กด `Ctrl + C`

---

## 5. การเพิ่มเพลงใหม่

### 5.1 เปิดไฟล์ songs.json

1. เปิดโฟลเดอร์ `leaves-notes-web`
2. เปิดไฟล์: `client → public → data → songs.json`
3. ใช้โปรแกรมอะไรก็ได้ที่เปิดไฟล์ข้อความได้ เช่น:
   - **Notepad** (มีใน Windows ทุกเครื่อง)
   - **VS Code** (แนะนำ — โหลดฟรีที่ https://code.visualstudio.com/)
   - **Sublime Text**

### 5.2 ทำความเข้าใจโครงสร้างเพลง 1 เพลง

แต่ละเพลงใน `songs.json` จะมีข้อมูลแบบนี้:

```json
{
  "id": "lao-duang-duen",
  "title": "ลาวดวงเดือน",
  "artist": "เพลงพื้นบ้านไทย",
  "difficulty": "beginner",
  "genre": "thai-traditional",
  "tags": ["ฟรี", "โน้ต"],
  "isFree": true,
  "pdfUrl": "/data/pdfs/lao-duang-duen.pdf",
  "description": "เพลงไทยเดิมที่คุ้นหูและเหมาะสำหรับผู้เริ่มต้น ทำนองไพเราะ จังหวะไม่เร็ว",
  "createdAt": "2024-01-01"
}
```

**คำอธิบายแต่ละช่อง:**

| ช่อง | ความหมาย | ค่าที่ใส่ได้ |
|------|----------|------------|
| `id` | รหัสเพลง (ไม่ซ้ำกัน) | ภาษาอังกฤษ ไม่มีวรรณยุกต์ ใช้ - คั่น เช่น `mai-mee-ter` |
| `title` | ชื่อเพลง | ภาษาไทยหรืออังกฤษก็ได้ |
| `artist` | ศิลปิน/ผู้แต่ง | เช่น "เพลงพื้นบ้านไทย", "Yiruma" |
| `difficulty` | ระดับความยาก | `"beginner"` = มือใหม่, `"intermediate"` = มือซอ, `"master"` = ปรมาจารย์ |
| `genre` | ประเภทเพลง | `"thai-traditional"` = ไทยเดิม, `"thai-modern"` = ไทย, `"international"` = สากล |
| `tags` | ป้ายกำกับ | เช่น `["ฟรี", "โน้ต"]` หรือ `["โน้ต", "คอร์ด", "คลิป"]` |
| `isFree` | ฟรีหรือไม่? | `true` = ฟรี, `false` = มีค่าใช้จ่าย |
| `pdfUrl` | ลิงค์ไฟล์ PDF | ถ้าฟรี: `"/data/pdfs/ชื่อไฟล์.pdf"` ถ้าไม่ฟรี: `""` (เว้นว่าง) |
| `description` | คำอธิบายสั้น ๆ | ข้อความอธิบายเพลง |
| `createdAt` | วันที่เพิ่ม | รูปแบบ `"ปี-เดือน-วัน"` เช่น `"2025-06-01"` |

### 5.3 ขั้นตอนการเพิ่มเพลง

**ตัวอย่าง:** ต้องการเพิ่มเพลง "ช่อผกากรอง" ศิลปิน "เพลงพื้นบ้านไทย" ระดับมือใหม่ ประเภทไทยเดิม ฟรี

**ขั้นตอนที่ 1:** เปิด `songs.json`

**ขั้นตอนที่ 2:** หาตำแหน่งที่ต้องการแทรก
- ภายในเครื่องหมาย `[` และ `]` (ก้ามปูใหญ่)
- แต่ละเพลงคั่นด้วย `,` (ลูกน้ำ)
- ควรเพิ่มต่อจากเพลงสุดท้าย ก่อนเครื่องหมาย `]`

**ขั้นตอนที่ 3:** เพิ่มข้อมูลเพลงใหม่ โดยใส่ `,` หลังเพลงสุดท้ายที่มีอยู่ แล้ววางข้อมูลใหม่:

```json
  // ... เพลงที่มีอยู่แล้วด้านบน ...
  {
    "id": "a-thousand-years",
    "title": "A Thousand Years",
    "artist": "Christina Perri",
    "difficulty": "beginner",
    "genre": "international",
    "tags": ["ฟรี", "โน้ต"],
    "isFree": true,
    "pdfUrl": "",
    "description": "เพลงสากลยอดนิยม เรียบเรียงสำหรับซอด้วงในระดับเริ่มต้น",
    "createdAt": "2024-03-15"
  },
  {
    "id": "cho-phak-kra-rong",
    "title": "ช่อผกากรอง",
    "artist": "เพลงพื้นบ้านไทย",
    "difficulty": "beginner",
    "genre": "thai-traditional",
    "tags": ["ฟรี", "โน้ต"],
    "isFree": true,
    "pdfUrl": "/data/pdfs/cho-phak-kra-rong.pdf",
    "description": "เพลงไทยเดิมทำนองไพเราะ เหมาะสำหรับผู้เริ่มต้นฝึกซอด้วง",
    "createdAt": "2025-06-01"
  }
]
```

⚠️ **ข้อควรระวัง:**
- เพลงสุดท้าย **ห้ามมี `,` (ลูกน้ำ) ต่อท้าย** ก่อนเครื่องหมาย `]`
- ทุกช่องต้องมี `,` ต่อท้าย **ยกเว้นช่องสุดท้าย** (`createdAt`)
- `id` ต้องไม่ซ้ำกับเพลงอื่น

**ขั้นตอนที่ 4:** บันทึกไฟล์ (Ctrl + S)

**ขั้นตอนที่ 5:** ถ้ากำลังรัน `pnpm dev` อยู่ — เว็บจะอัปเดตอัตโนมัติทันที!

### 5.4 ตัวอย่าง songs.json เต็ม (บางส่วน)

```json
{
  "songs": [
    {
      "id": "lao-duang-duen",
      "title": "ลาวดวงเดือน",
      "artist": "เพลงพื้นบ้านไทย",
      "difficulty": "beginner",
      "genre": "thai-traditional",
      "tags": ["ฟรี", "โน้ต"],
      "isFree": true,
      "pdfUrl": "/data/pdfs/lao-duang-duen.pdf",
      "description": "เพลงไทยเดิมที่คุ้นหูและเหมาะสำหรับผู้เริ่มต้น ทำนองไพเราะ จังหวะไม่เร็ว",
      "createdAt": "2024-01-01"
    }
  ]
}
```

---

## 6. การแก้ไขเพลงที่มีอยู่

### 6.1 เปลี่ยนชื่อเพลง

1. เปิด `songs.json`
2. หาเพลงที่ต้องการ (ค้นหาจาก `id` หรือ `title`)
3. เปลี่ยนข้อความในช่อง `"title": "..."`

**ตัวอย่าง:** เปลี่ยน "ลาวดวงเดือน" → "ลาวดวงเดือน (ฉบับปรับปรุง)"
```json
{
  "id": "lao-duang-duen",
  "title": "ลาวดวงเดือน (ฉบับปรับปรุง)",   // ← แก้ตรงนี้
  ...
}
```

### 6.2 เปลี่ยนระดับความยาก

เปลี่ยนค่าที่ `"difficulty"`:
- `"beginner"` → มือใหม่ก็เล่นได้
- `"intermediate"` → มือซอ
- `"master"` → ระดับปรมาจารย์

### 6.3 เปลี่ยนจากเพลงฟรีเป็นมีค่าใช้จ่าย (หรือกลับกัน)

เปลี่ยน `"isFree"`:
- `true` → ฟรี (คนโหลดได้เลย)
- `false` → มีค่าใช้จ่าย (ต้องติดต่อ inbox)

### 6.4 เปลี่ยนลิงค์ PDF

เปลี่ยน `"pdfUrl"`:
- ถ้าฟรี: `"/data/pdfs/ชื่อไฟล์.pdf"` (วาง PDF ใน `public/pdfs/`)
- ถ้าไม่ฟรี: `""` (เว้นว่าง)

### 6.5 เปลี่ยนคำอธิบาย

เปลี่ยนข้อความใน `"description"` — พิมพ์อะไรก็ได้

---

## 7. การลบเพลง

1. เปิด `songs.json`
2. หาเพลงที่ต้องการลบ
3. ลบตั้งแต่เครื่องหมาย `{` จนถึง `}` ของเพลงนั้น **รวมทั้ง `,` (ลูกน้ำ) ด้านหลังหรือด้านหน้าด้วย**
4. บันทึกไฟล์

**ตัวอย่าง:** ลบเพลง "สามชั้น"
```json
// ก่อนลบ
  {
    "id": "sam-chan",
    "title": "สามชั้น",
    ...
    "createdAt": "2024-01-20"
  },          ← ลบลูกน้ำนี้ด้วย
  {
    "id": "lao-duang-duen",   ← เพลงถัดไป
    ...
  },

// หลังลบ
  {
    "id": "lao-duang-duen",   ← เพลงถัดไป (ไม่มีลูกน้ำค้าง)
    ...
  },
```

⚠️ **ระวัง:** อย่าลืมลบ `,` (ลูกน้ำ) ด้วย ไม่เช่นนั้นไฟล์จะเสีย

---

## 8. การเปลี่ยนราคาแพ็กเกจ

### 8.1 เปิดไฟล์ packages.json

`client → public → data → packages.json`

### 8.2 โครงสร้างของ 1 แพ็กเกจ

```json
{
  "id": "package-1",
  "title": "แบบที่ 1",
  "subtitle": "โน้ตเมโลดี้",
  "description": "เหมาะสำหรับผู้ต้องการเล่นเฉพาะ Melody ดูง่าย ไม่ซับซ้อน เหมาะสำหรับผู้เริ่มต้น",
  "price": 200,
  "features": ["โน้ตเมโลดี้", "เนื้อเพลง", "ไฟล์ PDF"],
  "isHighlighted": false
}
```

| ช่อง | ความหมาย |
|------|----------|
| `id` | รหัสแพ็กเกจ (ไม่ซ้ำ) |
| `title` | ชื่อแพ็กเกจ |
| `subtitle` | คำอธิบายสั้น |
| `description` | คำอธิบายยาว |
| `price` | ราคา (ตัวเลขเท่านั้น ไม่ต้องมีเครื่องหมายบาท) |
| `features` | รายการสิ่งที่ได้ (คั่นด้วย `,`) |
| `isHighlighted` | `true` = แนะนำ (การ์ดจะเด่น), `false` = ปกติ |

### 8.3 เปลี่ยนราคา

หา `"price": 200` → เปลี่ยนตัวเลข เช่น `"price": 250`

### 8.4 เปลี่ยนคำอธิบาย

เปลี่ยนข้อความใน `"description"` หรือ `"features"`

**ตัวอย่าง:** เพิ่ม "คลิปสาธิต" ในแบบที่ 2
```json
{
  "id": "package-2",
  ...
  "features": ["โน้ตเมโลดี้", "คอร์ดประกอบ", "เนื้อเพลง", "ไฟล์ PDF", "คลิปสาธิต"],
  ...
}
```

---

## 9. การเพิ่ม/ลบแพ็กเกจ

### 9.1 เพิ่มแพ็กเกจใหม่

เพิ่ม object ใหม่ใน `packages.json` ภายในเครื่องหมาย `[` และ `]`:

```json
{
  "id": "package-4",
  "title": "แบบที่ 4",
  "subtitle": "Melody + Chord + Video + ตัวต่อตัว",
  "description": "สอนตัวต่อตัวผ่านออนไลน์ พร้อมโน้ตและคลิป",
  "price": 800,
  "features": ["โน้ตเมโลดี้", "คอร์ดประกอบ", "เนื้อเพลง", "ไฟล์ PDF", "คลิปสาธิต", "สอนออนไลน์"],
  "isHighlighted": false
}
```

### 9.2 ลบแพ็กเกจ

ลบ object ของแพ็กเกจนั้นออก (เหมือนลบเพลง — ระวัง `,`)

### 9.3 เปลี่ยนแพ็กเกจที่แนะนำ (Highlight)

เปลี่ยน `"isHighlighted"`:
- `true` = การ์ดเด่น (มีขอบสี)
- `false` = การ์ดปกติ

**ควรมีแค่ 1 แพ็กเกจที่ `true`**

---

## 10. การเปลี่ยนรูปภาพ

### 10.1 รูป Hero (รูปใหญ่ด้านบน)

**ไฟล์:** `client/src/assets/Cover2.png`

**วิธีเปลี่ยน:**
1. เตรียมรูปใหม่ (แนะนำขนาด 800x600px, นามสกุล .png หรือ .jpg)
2. วางรูปใหม่ทับไฟล์ `Cover2.png` (ใช้ชื่อเดียวกัน)
3. หรือจะเปลี่ยนชื่อไฟล์ก็ได้ แต่ต้องแก้โค้ดใน `Home.tsx`

**วิธีเปลี่ยนชื่อไฟล์ (สำหรับคนที่อยากใช้ชื่ออื่น):**
1. วางรูปใหม่ใน `client/src/assets/` เช่น `my-new-cover.jpg`
2. เปิดไฟล์ `client/src/pages/Home.tsx`
3. หาบรรทัด: `import Cover from "@/assets/Cover2.png";`
4. เปลี่ยนเป็น: `import Cover from "@/assets/my-new-cover.jpg";`
5. บันทึก

### 10.2 รูปพื้นหลัง (Texture กระดาษ)

ปัจจุบันใช้ URL จาก CDN ภายนอก ถ้าอยากเปลี่ยน:
1. เปิด `client/src/pages/Home.tsx`
2. หาบรรทัด: `const PAPER_BG = "https://...";`
3. เปลี่ยน URL เป็นรูปที่ต้องการ

### 10.3 รูปปกเพลง (Song Image)

ปัจจุบันเพลงยังไม่มีรูปปก ถ้าอยากเพิ่มในอนาคต:
1. วางรูปใน `client/public/images/`
2. เพิ่มช่อง `"imageUrl": "/images/ชื่อรูป.jpg"` ใน `songs.json`

---

## 11. การเปลี่ยนลิงค์ Social Media

### 11.1 เปิดไฟล์ contacts.json

`client → public → data → contacts.json`

### 11.2 โครงสร้าง

```json
{
  "facebook": "https://www.facebook.com/",
  "line": "",
  "instagram": "",
  "youtube": "",
  "tiktok": "",
  "email": "",
  "deliveryTime": "ประมาณ 1 สัปดาห์",
  "deliveryMethod": "ไฟล์ PDF ส่งทาง Inbox"
}
```

### 11.3 วิธีเปลี่ยน

| ช่อง | ใส่ลิงค์แบบนี้ |
|------|--------------|
| `facebook` | `"https://www.facebook.com/ชื่อเพจ"` |
| `line` | `"https://line.me/R/ti/p/~ไอดีไลน์"` |
| `instagram` | `"https://www.instagram.com/ชื่อไอดี/"` |
| `youtube` | `"https://www.youtube.com/@ชื่อช่อง"` |
| `tiktok` | `"https://www.tiktok.com/@ชื่อไอดี"` |
| `email` | `"ชื่อ@อีเมล.com"` |

**ถ้าช่องไหนยังไม่มี ให้ใส่ `""` (เว้นว่าง) — เว็บจะไม่แสดงช่องนั้น**

### 11.4 เปลี่ยนเวลาจัดส่ง

เปลี่ยน `"deliveryTime"` เช่น `"ประมาณ 3-5 วันทำการ"`

---

## 12. การเปลี่ยนสีและฟอนต์

### 12.1 เปิดไฟล์ index.css

`client → src → index.css`

### 12.2 สีหลักของเว็บ

หาโค้ดส่วนนี้:

```css
:root {
  --radius: 0.25rem;
  --background: oklch(0.975 0.012 85);   /* สีพื้นหลัง */
  --foreground: oklch(0.18 0.04 40);     /* สีตัวอักษร */
  --primary: oklch(0.35 0.1 35);         /* สีหลัก (แดงหมึก) */
  --border: oklch(0.83 0.04 80);         /* สีขอบ */
  ...
}
```

**วิธีเปลี่ยนสี:**

ใช้รหัสสีแบบ **HEX** (ที่ขึ้นต้นด้วย #) แทน oklch ก็ได้:

```css
:root {
  --background: #F7F2E7;    /* เปลี่ยนสีพื้นหลัง */
  --foreground: #2C1810;    /* เปลี่ยนสีตัวอักษร */
  --primary: #8B3A2A;       /* เปลี่ยนสีหลัก */
  --border: #D4C5A0;        /* เปลี่ยนสีขอบ */
}
```

**หาโค้ดสีได้ที่:** https://colorhunt.co/ หรือ https://coolors.co/

### 12.3 ฟอนต์

ฟอนต์ปัจจุบัน:
- **หัวข้อ:** `Noto Serif Thai` (ฟอนต์หัวข้อ สง่างาม)
- **เนื้อหา:** `Sarabun` (ฟอนต์อ่านง่าย)

**วิธีเปลี่ยนฟอนต์:**

**ขั้นตอนที่ 1:** เปิด `client/index.html`
**ขั้นตอนที่ 2:** หาบรรทัด:
```html
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+Thai:wght@400;600;700;900&family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
```
**ขั้นตอนที่ 3:** เปลี่ยนชื่อฟอนต์ เช่น เป็น `Prompt`:
```html
<link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
```
**ขั้นตอนที่ 4:** เปิด `client/src/index.css` → หา `font-family` → เปลี่ยนชื่อฟอนต์

---

## 13. การเปลี่ยนข้อความในหน้าเว็บ

ข้อความส่วนใหญ่ที่คนเห็น (หัวข้อ, คำอธิบาย, ปุ่ม) อยู่ในไฟล์:

| ข้อความ | ไฟล์ |
|---------|------|
| หัวข้อ "โน้ตเพลงจากใบบุญเอง" | `client/src/components/sections/HeroSection.tsx` |
| ข้อความ "เป็นเด็กวิศวะคอม..." | `client/src/components/sections/HeroSection.tsx` |
| "รวมโน้ตเพลง" | `client/src/components/sections/SongSection.tsx` |
| "สั่งทำโน้ต" | `client/src/components/sections/OrderSection.tsx` |
| ข้อความท้ายเว็บ (Footer) | `client/src/components/layout/Footer.tsx` |
| ป้ายระดับความยาก | `client/public/data/songs.json` (field: difficulty) |
| ชื่อแพ็กเกจ | `client/public/data/packages.json` |

**วิธีแก้:** เปิดไฟล์ → หาข้อความ → พิมพ์ทับ → บันทึก

---

## 14. การเพิ่มไฟล์ PDF โน้ตเพลง

### 14.1 วางไฟล์ PDF

1. เปิดโฟลเดอร์ `leaves-notes-web`
2. ไปที่ `client → public → pdfs`
3. วางไฟล์ PDF ที่นี่

### 14.2 ตั้งชื่อไฟล์

ใช้ชื่อภาษาอังกฤษ ไม่มีวรรณยุกต์ ใช้ `-` คั่นระหว่างคำ เช่น:
- `lao-duang-duen.pdf` ✅
- `mai-mee-ter-mai-dai.pdf` ✅
- `ลาวดวงเดือน.pdf` ❌ (ชื่อไทยอาจมีปัญหากับลิงค์)

### 14.3 เชื่อมโยงกับเพลงใน songs.json

เปิด `songs.json` → หาเพลงที่ต้องการ → เปลี่ยน `"pdfUrl"`:

```json
{
  "id": "lao-duang-duen",
  ...
  "pdfUrl": "/data/pdfs/lao-duang-duen.pdf",   // ← เปลี่ยนตรงนี้
  ...
}
```

**รูปแบบ:** `"/data/pdfs/ชื่อไฟล์.pdf"` (ขึ้นต้นด้วย `/data/pdfs/` เสมอ)

### 14.4 ถ้าเพลงนั้นไม่ฟรี (มีค่าใช้จ่าย)

ให้ใส่ `"pdfUrl": ""` (เว้นว่าง) — คนจะเห็นปุ่ม "ติดต่อสั่งซื้อ" แทนปุ่ม "ดาวน์โหลด"

---

## 15. การ Deploy ขึ้น Netlify (ทำให้คนอื่นดูได้)

### 15.1 สมัคร Netlify

1. ไปที่ https://app.netlify.com/
2. กด **Sign up**
3. เลือก **GitHub** (ใช้บัญชี GitHub เดียวกับที่เก็บโค้ด)
4. อนุญาตให้ Netlify เชื่อมต่อ

### 15.2 เชื่อมต่อกับ GitHub

1. กด **Add new site** → **Import an existing project**
2. เลือก **GitHub**
3. ค้นหา `leaves-notes-web`
4. กดเลือก

### 15.3 ตั้งค่า (ไม่ต้องแก้ เพราะมี netlify.toml แล้ว)

Netlify จะอ่านค่าจาก `netlify.toml` โดยอัตโนมัติ:
- **Build command:** `pnpm build`
- **Publish directory:** `dist/public`

### 15.4 กด Deploy

1. กด **Deploy site**
2. รอประมาณ 1-2 นาที
3. เสร็จ! จะได้ลิงค์ประมาณ: `https://ชื่อสุ่ม.netlify.app/`

### 15.5 เปลี่ยนชื่อเว็บ (ไม่ต้องเป็นชื่อสุ่ม)

1. ไปที่ **Site settings** → **Change site name**
2. พิมพ์ชื่อที่ต้องการ เช่น `saodung-notes`
3. จะได้ลิงค์: `https://saodung-notes.netlify.app/`

### 15.6 การอัปเดตเว็บ (เมื่อแก้ไขอะไรแล้ว)

**วิธีที่ 1: Push ไป GitHub (อัตโนมัติ)**
1. แก้ไขไฟล์เสร็จ
2. เปิด Terminal ในโฟลเดอร์ `leaves-notes-web`
3. พิมพ์:
   ```
   git add .
   git commit -m "อัปเดตเพลง"
   git push
   ```
4. Netlify จะ build และ deploy อัตโนมัติ (~1-2 นาที)

**วิธีที่ 2: ลากวาง (Drag & Drop)**
1. รัน `pnpm build` บนเครื่อง
2. จะได้โฟลเดอร์ `dist/public`
3. ไปที่ Netlify → ลากโฟลเดอร์ `public` วาง

---

## 16. การแก้ไขปัญหาเบื้องต้น

### ปัญหา: เว็บไม่ขึ้นหลังจากแก้ JSON

**สาเหตุ:** อาจลืม `,` หรือใส่ `,` เกิน

**วิธีแก้:**
1. เปิด https://jsonlint.com/
2. คัดลอกเนื้อหา `songs.json` ทั้งหมด
3. วางในช่องว่าง
4. กด **Validate JSON**
5. ถ้ามี error จะบอกบรรทัดที่ผิด

### ปัญหา: `pnpm dev` ไม่ทำงาน

**สาเหตุ:** อาจยังไม่ได้ติดตั้ง dependencies

**วิธีแก้:**
```
pnpm install
pnpm dev
```

### ปัญหา: `git push` ไม่ได้

**สาเหตุ:** อาจยังไม่ได้ตั้งค่า Git

**วิธีแก้:**
```
git config --global user.name "ชื่อคุณ"
git config --global user.email "อีเมลคุณ"
```

### ปัญหา: รูปไม่ขึ้นบนเว็บ

**สาเหตุ:** path รูปผิด หรือไฟล์ไม่อยู่ในที่ที่ถูก

**วิธีแก้:**
- ตรวจสอบว่าไฟล์อยู่ใน `client/public/pdfs/` หรือ `client/src/assets/`
- ตรวจสอบชื่อไฟล์ (ตัวพิมพ์ใหญ่-เล็ก ต้องตรงกัน)

### ปัญหา: Netlify Deploy ล้มเหลว

**สาเหตุ:** อาจมี error ในโค้ด

**วิธีแก้:**
1. ไปที่ Netlify → Deploys → คลิก deploy ที่ failed
2. อ่าน error log — จะบอกว่าผิดตรงไหน
3. แก้ไขตามที่ error บอก → push ใหม่

---

## 17. Glossary — คำศัพท์ที่ควรรู้

| คำศัพท์ | คำอ่าน | ความหมาย |
|---------|--------|---------|
| **Repository (Repo)** | รีพอสิทอรี | ที่เก็บโค้ดและไฟล์ทั้งหมดของโปรเจกต์ |
| **Git** | กิต | ระบบบันทึกประวัติการแก้ไขไฟล์ |
| **GitHub** | กิตฮับ | เว็บไซต์ฝาก repo ออนไลน์ |
| **Commit** | คอมมิต | การบันทึกการเปลี่ยนแปลง (เหมือนเซฟเกม) |
| **Push** | พุช | การอัปโหลด commit ขึ้น GitHub |
| **Pull** | พุล | การดาวน์โหลดโค้ดล่าสุดจาก GitHub |
| **Deploy** | ดิพลอย | การทำให้เว็บขึ้นอินเทอร์เน็ตให้คนอื่นดูได้ |
| **Local** | โลคอล | บนเครื่องตัวเอง (ยังไม่ขึ้นอินเทอร์เน็ต) |
| **JSON** | เจสัน | รูปแบบไฟล์เก็บข้อมูล (อ่านง่าย เหมือนสมุดจด) |
| **Terminal / CMD** | เทอร์มินอล | หน้าต่างดำ ๆ สำหรับพิมพ์คำสั่ง |
| **Dependencies** | ดีเพนเดนซีส์ | เครื่องมือที่เว็บต้องใช้ (ติดตั้งด้วย pnpm install) |
| **Build** | บิลด์ | การแปลงโค้ดให้เป็นเว็บที่พร้อมใช้งาน |
| **CDN** | ซีดีเอ็น | ระบบกระจายไฟล์ไปทั่วโลกให้โหลดเร็ว |
| **Path** | พาธ | ที่อยู่ของไฟล์ เช่น `/data/pdfs/song.pdf` |
| **Root** | รูท | โฟลเดอร์หลักของโปรเจกต์ |
| **Array** | อาร์เรย์ | รายการของสิ่งของ อยู่ในเครื่องหมาย `[ ]` |
| **Object** | ออบเจกต์ | ข้อมูลของสิ่งเดียว อยู่ในเครื่องหมาย `{ }` |
| **Key / Value** | คี / แวลู | ช่องข้อมูล / ค่าของช่องนั้น เช่น `"title": "ลาวดวงเดือน"` |
| **Boolean** | บูลีน | ค่าที่เป็น `true` (จริง/ใช่) หรือ `false` (เท็จ/ไม่ใช่) |

---

## 📌 สรุปไฟล์ที่ต้องจำ

| ไฟล์ | ไว้ทำอะไร |
|------|----------|
| `client/public/data/songs.json` | ★★★ แก้ข้อมูลเพลง ★★★ |
| `client/public/data/packages.json` | แก้ราคาแพ็กเกจ |
| `client/public/data/contacts.json` | แก้ลิงค์ Social Media |
| `client/public/pdfs/` | วางไฟล์ PDF โน้ตเพลง |
| `client/src/index.css` | เปลี่ยนสีและฟอนต์ |
| `client/src/components/sections/HeroSection.tsx` | เปลี่ยนข้อความ Hero |
| `client/src/components/sections/SongSection.tsx` | เปลี่ยนข้อความส่วนเพลง |
| `client/src/components/sections/OrderSection.tsx` | เปลี่ยนข้อความส่วนสั่งทำ |
| `client/src/components/layout/Footer.tsx` | เปลี่ยนข้อความท้ายเว็บ |

---

## 🎯 Quick Reference — ถ้าอยากทำอะไร ให้ทำยังไง

| อยากทำ | เปิดไฟล์นี้ | แก้ตรงนี้ |
|--------|-----------|---------|
| เพิ่มเพลงใหม่ | `songs.json` | เพิ่ม object ใหม่ใน `"songs": [...]` |
| เปลี่ยนชื่อเพลง | `songs.json` | `"title": "..."` |
| เปลี่ยนราคา | `packages.json` | `"price": ...` |
| เปลี่ยนลิงค์ Facebook | `contacts.json` | `"facebook": "..."` |
| เปลี่ยนรูป Hero | `Cover2.png` ใน `assets/` | วางรูปทับ |
| เปลี่ยนสีเว็บ | `index.css` | ค่า `--primary`, `--background` ฯลฯ |
| เปลี่ยนฟอนต์ | `index.html` + `index.css` | ลิงค์ Google Fonts + font-family |
| เพิ่ม PDF | `public/pdfs/` | วางไฟล์ + แก้ `pdfUrl` ใน `songs.json` |
| Deploy ขึ้นเน็ต | Netlify | Import repo → auto deploy |
| อัปเดตเว็บ | Terminal | `git add .` → `git commit` → `git push` |

---

**🎉 ขอให้สนุกกับการทำเว็บซอด้วง โน้ตนะคะ!**

*ถ้ามีคำถามเพิ่มเติม ทักมาได้เลย — ไอริสยินดีช่วยเสมอ 💚*